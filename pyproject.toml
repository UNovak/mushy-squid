[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mushy-squid"
version = "0.1.0"
description = "CVRP solver"
dependencies = [
    "numpy",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
